/**
 * Reset Password Route
 *
 * Entry point for the reset password screen.
 * All logic and UI is in src/auth/reset-password.
 */

import { ResetPasswordScreen } from '@/src/auth/reset-password/components/ResetPasswordScreen';

export default ResetPasswordScreen;
