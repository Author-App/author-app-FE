/**
 * Verification Code Route
 *
 * Entry point for the verification code screen.
 * All logic and UI is in src/auth/verification-code.
 */

import { VerificationCodeScreen } from '@/src/auth/verification-code/components/VerificationCodeScreen';

export default VerificationCodeScreen;
